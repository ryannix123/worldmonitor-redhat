# Pinned upstream commit. Bumped by .github/workflows/upstream-sync.yml via PR.
#
# Upstream tags stopped ~5 months ago while commits continue daily, so there is
# no meaningful release to track — v2.5.23 is a stale marker, not a chosen
# release. Pinning a SHA gives reproducible builds plus a reviewed upgrade path.
UPSTREAM_REF=main
